# Source materials — Problem Selection

- `ASQ-WhatIsAnAffinityDiagram-KJMethod.pdf` — ASQ's "What is an Affinity
  Diagram? (K-J Method)" page (adapted from *The Quality Toolbox*, 2nd ed.,
  ASQ Quality Press). Machine-readable transcription under
  `extracted/asq-affinity-diagram.txt` (navigation and advertising
  stripped; the example-3 table reconstructed by column). All rights remain
  with ASQ.
- The **"How to pick the right problem"** framework (Customer Signal ×
  Business Alignment; the four-step problem-statement → evidence → patterns
  → validation execution) arrived as the PM's working notes, not as a
  document — it is distilled directly in
  [`../picking-the-right-problem.md`](../picking-the-right-problem.md).
  If those notes trace to a published course or article, add the citation
  there and in `CREDITS.md`.

Add anything else that should ground the agent: your own notes, examples of
evidence tables you've built, prioritization matrices from past quarters.
Supported formats the agent can read directly: `.md`, `.txt`, `.pdf` (this
environment can't render PDFs — put text under `extracted/`).

When you add something here, fold the specifics into the method docs (the
agent reads those) and cite the source under their **Sources & materials**
section.
